<resources>

    <style name="AppTheme" parent="@style/Theme.AppCompat.DayNight">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:forceDarkAllowed">true</item>
    </style>

    <style name="Theme.AppCompat.DayNight.FullScreen.Actionbar" parent="Theme.AppCompat.DayNight">
        <item name="android:windowFullscreen">true</item>
        <item name="android:forceDarkAllowed">true</item>
    </style>

    <style
            name="Theme.AppCompat.Light.NoActionBar.FullScreen.AppBarOverlay"
           parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style
            name="Theme.AppCompat.Light.NoActionBar.FullScreen.PopupOverlay"
           parent="ThemeOverlay.AppCompat.Light"/>

</resources>